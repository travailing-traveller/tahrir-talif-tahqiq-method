# registers/

Distilled outputs derived from **verified** units — scholarly apparatus, sermon,
audio script, simplified explainer, slides, translation, etc.

A register may compress, reorder, translate, and omit. It may **never add**
propositional content absent from the verified core. Keep a claim-to-source map
beside each register so review targets only new claims (there should be none).
