# Claude adapter

Claude Code should read `AGENTS.md` first and treat it as canonical for the
project's rules and the Taḥrīr / Taʾlīf / Taḥqīq workflow. Work from the active
sprint under `sprints/` and the active unit under `units/`. Verify with
`scripts/verify-unit.py`. Only useful if you run method roles inside an agentic
tool; chat-driven use can ignore this file.
