# Codex adapter

Codex should read `AGENTS.md` first and treat it as canonical. Build from the
active sprint under `sprints/` (requirements, blueprint, acceptance, handoff) and
the active unit under `units/`. Verify with `scripts/verify-unit.py`.
