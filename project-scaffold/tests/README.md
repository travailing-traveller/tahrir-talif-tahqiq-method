# tests/

Tests for the Builder's code. A transformation must have a check asserting it did
only what it declares (cf. `scripts/normalise-arabic.py --check`). Test through
interfaces, on behaviour not implementation.
