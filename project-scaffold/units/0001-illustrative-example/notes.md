# Unit 0001 — Illustrative Example

This is synthetic and should be deleted in a real project.

The second span intentionally includes a diacritic difference so that the verification harness reports `REVIEW` rather than silently accepting it as exact.
