# Sprint NNN — <name> — Handoff

You are taking Sprint NNN. Read `AGENTS.md`, then `PROJECT.md`, `STATE.md`,
`CONVENTIONS.md`, and this sprint's `requirements`/`blueprint`/`acceptance`.

Active unit(s): … . Compose with `method-kit/06`, verify with `method-kit/06`
(ideally a different model), run `scripts/verify-unit.py`. Stay within scope; log
anything uncertain.
