# Sprint NNN — <name> — Requirements

## Goal
One sentence: what this stage delivers.

## Units in scope
- `units/NNNN-<slug>` … (a range or explicit list)

## Sources
- which `corpus/<handle>.txt` files this stage draws on

## Layer / register
- the layer being added (base text / lexical / balāgha / translation …) or the
  register being produced

## Out of scope
- what this stage deliberately excludes
