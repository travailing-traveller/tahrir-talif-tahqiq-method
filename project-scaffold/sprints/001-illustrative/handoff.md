# Sprint 001 — Illustrative — Handoff

You are taking Sprint 001. Read `AGENTS.md`, then `PROJECT.md`, `STATE.md`,
`CONVENTIONS.md`, and this sprint's `requirements`/`blueprint`/`acceptance`.

Active unit: `units/0001-illustrative-example`. Compose with `method-kit/06`,
verify with `method-kit/06` (ideally on a different model), and run
`scripts/verify-unit.py`. Do not exceed the units in scope.
